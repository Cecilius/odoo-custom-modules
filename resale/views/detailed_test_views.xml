<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_resale_detailed_test_tree" model="ir.ui.view">
        <field name="name">resale.detailed.test.tree</field>
        <field name="model">resale.detailed.test</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="product_id"/>
                <field name="product_rfb"/>
                <field name="user_id"/>
                <field name="date"/>
                <field name="state"/>
                <field name="final_disposition"/>
            </list>
        </field>
    </record>

    <record id="view_resale_detailed_test_form" model="ir.ui.view">
        <field name="name">resale.detailed.test.form</field>
        <field name="model">resale.detailed.test</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_complete" string="Complete Test" type="object"
                            class="oe_highlight" invisible="state == 'done'"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,done"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name" readonly="1"/></h1>
                    </div>
                    <group string="Item">
                        <field name="product_id" readonly="1"/>
                        <field name="product_name" readonly="1"/>
                        <field name="product_rfb" readonly="1"/>
                    </group>
                    <group>
                        <field name="user_id" readonly="state == 'done'"/>
                        <field name="test_type_id" readonly="state == 'done'"/>
                        <field name="result" readonly="state == 'done'"/>
                        <field name="final_condition_id" readonly="state == 'done'"/>
                        <field name="final_functional_status" readonly="state == 'done'"/>
                        <field name="final_disposition" readonly="state == 'done'"/>
                        <field name="comment" readonly="state == 'done'"/>
                        <field name="date" readonly="1"/>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="action_resale_detailed_test" model="ir.actions.act_window">
        <field name="name">Detailed Tests</field>
        <field name="res_model">resale.detailed.test</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
